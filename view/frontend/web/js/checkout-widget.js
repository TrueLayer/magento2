/*global define*/
define([
    'truelayer/web-sdk',
    'jquery',
    'uiComponent',
    'Magento_Ui/js/lib/view/utils/dom-observer'
], function (webSdk, jQuery, Component, observer) {
    'use strict';

    return Component.extend({
        initialize: function () {

            // Mount sdk
            // Create payment
            // Start


            // observer.get('#truelayer-payment-method', function (container) {
            //     webSdk.initWebSdk({})
            //         .mount(container)
            //         .start({})
            //     return this;
            // });
        }
    });
});